{
  "name": "coach",
  "private": true,
  "scripts": {
    "build:css": "npx tailwindcss -c tailwind.config.js -i src/tailwind.css -o tailwind.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/container-queries": "^0.1",
    "@tailwindcss/forms": "^0.5",
    "tailwindcss": "^3"
  }
}
